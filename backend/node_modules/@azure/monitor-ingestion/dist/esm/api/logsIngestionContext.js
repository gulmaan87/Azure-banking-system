// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { __rest } from "tslib";
import { logger } from "../logger.js";
import { KnownMonitorAudience } from "../models/models.js";
import { getClient } from "@azure-rest/core-client";
/** Azure Monitor data collection client. */
export function createLogsIngestion(endpoint, tokenCredential, options = {}) {
    var _a, _b, _c, _d, _e, _f, _g;
    const endpointUrl = (_b = (_a = options.endpoint) !== null && _a !== void 0 ? _a : options.baseUrl) !== null && _b !== void 0 ? _b : String(endpoint);
    const prefixFromOptions = (_c = options === null || options === void 0 ? void 0 : options.userAgentOptions) === null || _c === void 0 ? void 0 : _c.userAgentPrefix;
    const userAgentInfo = `azsdk-js-monitor-ingestion/1.2.0`;
    const userAgentPrefix = prefixFromOptions
        ? `${prefixFromOptions} azsdk-js-api ${userAgentInfo}`
        : `azsdk-js-api ${userAgentInfo}`;
    const scope = (options === null || options === void 0 ? void 0 : options.audience)
        ? `${options.audience}/.default`
        : `${KnownMonitorAudience.AzurePublicCloud}/.default`;
    const _h = Object.assign(Object.assign({}, options), { userAgentOptions: { userAgentPrefix }, loggingOptions: { logger: (_e = (_d = options.loggingOptions) === null || _d === void 0 ? void 0 : _d.logger) !== null && _e !== void 0 ? _e : logger.info }, credentials: {
            scopes: scope ? [scope] : (_f = options.credentials) === null || _f === void 0 ? void 0 : _f.scopes,
        } }), { apiVersion: _ } = _h, updatedOptions = __rest(_h, ["apiVersion"]);
    const clientContext = getClient(endpointUrl, tokenCredential, updatedOptions);
    clientContext.pipeline.removePolicy({ name: "ApiVersionPolicy" });
    const apiVersion = (_g = options.apiVersion) !== null && _g !== void 0 ? _g : "2023-01-01";
    clientContext.pipeline.addPolicy({
        name: "ClientApiVersionPolicy",
        sendRequest: (req, next) => {
            // Use the apiVersion defined in request url directly
            // Append one if there is no apiVersion and we have one at client options
            const url = new URL(req.url);
            if (!url.searchParams.get("api-version")) {
                req.url = `${req.url}${Array.from(url.searchParams.keys()).length > 0 ? "&" : "?"}api-version=${apiVersion}`;
            }
            return next(req);
        },
    });
    return Object.assign(Object.assign({}, clientContext), { apiVersion });
}
//# sourceMappingURL=logsIngestionContext.js.map